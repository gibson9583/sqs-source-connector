{
    "name": "sqs-connector-webadmin",
    "version": "1.0.0",
    "private": true,
    "description": "Build for the SQS Connector web admin plugin (compiles web/plugin.jsx -> web/plugin.js).",
    "type": "module",
    "scripts": {
        "build": "node web/build.mjs",
        "test": "node --experimental-vm-modules --test test/*.test.mjs"
    },
    "devDependencies": {
        "esbuild": "0.25.0"
    },
    "license": "MPL-2.0"
}
